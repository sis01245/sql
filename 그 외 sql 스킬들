#set은 변수를 설정할 수 있게 도와준다.
SET @변수이름 := 대입값 으로 변수 설정이 가능하다.
이 변수를 table의 select문에서 사용하면
select (@HOUR := @HOUR) as HOUR from animal_outs 
where @HOUR < 23
이런 식으로 animal_outs 테이블의 가장 왼쪽에 HOUR 필드를 만들 수 있다.
